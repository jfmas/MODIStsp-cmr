% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_mod_granules_cmr.R
\name{get_mod_granules_cmr}
\alias{get_mod_granules_cmr}
\title{Query NASA CMR for MODIS granules}
\usage{
get_mod_granules_cmr(
  short_name,
  version,
  date_from,
  date_to,
  v = NULL,
  h = NULL,
  tiled = 1,
  n_retries = 20
)
}
\arguments{
\item{short_name}{\code{character} MODIS product short name (e.g. "MOD13Q1").}

\item{version}{\code{character} product version/collection (e.g. "061").}

\item{date_from}{\code{character} start date, format "YYYY.MM.DD" or "YYYY-MM-DD".}

\item{date_to}{\code{character} end date, format "YYYY.MM.DD" or "YYYY-MM-DD".}

\item{v}{\code{integer array} vertical tiles of interest (e.g. \code{c(4, 5)}). Ignored
when \code{tiled == 0}. Default: NULL.}

\item{h}{\code{integer array} horizontal tiles of interest (e.g. \code{c(17, 18)}).
Ignored when \code{tiled == 0}. Default: NULL.}

\item{tiled}{\code{numeric [0/1]} 1 = tiled product (filter by \code{hXXvYY}); 0 =
non-tiled/global product. Default: 1.}

\item{n_retries}{\code{numeric} number of times a failing CMR request is retried
before aborting. Default: 20.}
}
\value{
\code{data.frame} with columns \code{title}, \code{date} ("YYYY.MM.DD"), \code{hdf_name}
and \code{url}. Empty (0-row) data.frame if no granule matches.
}
\description{
Accessory function that queries the NASA Common Metadata
Repository (CMR) for the MODIS granules of a given product / version,
temporal range and (optionally) set of MODIS tiles, and returns their
acquisition dates, HDF file names and Earthdata Cloud download URLs.

This replaces the historical approach of scraping HTML directory listings
from the LP DAAC Data Pool (\code{e4ftl01.cr.usgs.gov}), which was retired for
MODIS on 2025-06-30. MODIS Collection 6.1 is now distributed through the
NASA Earthdata Cloud and discoverable via CMR (provider \code{LPCLOUD}).
}
\details{
Server-side tile filtering uses CMR's \code{readable_granule_name}
pattern option (\code{*.hXXvYY.*}), which keeps queries small. Results larger
than one page are retrieved following the \code{CMR-Search-After} header.
}
\note{
License: GPL 3.0
}
\author{
Lorenzo Busetto, phD (2014-2017)

Luigi Ranghetti, phD (2016-2017)
}
\keyword{internal}
